#include <stdio.h>
#include <string.h>

int main ()
{
  char s[100];
  int length,i,count=1;
  scanf("%s",s);
  length=strlen(s);

  for(i=0;i<length;i++)
  {
    if(s[i]==',')
    count++;
  }
  printf("%d\n",count);
  return 0;
}
